# Template only. Do not add this file to kustomization.yaml and do not apply it.
# scripts/deploy_k8s.ps1 creates british-museum-secrets from process environment variables.
apiVersion: v1
kind: Secret
metadata:
  name: british-museum-secrets
type: Opaque
stringData:
  STAFF_DEMO_PASSWORD: REPLACE_ME
  JWT_SECRET: REPLACE_ME
  MCP_INTERNAL_TOKEN: REPLACE_ME
  GOOGLE_API_KEY: REPLACE_ME_OPTIONAL
  GEMINI_API_KEY: REPLACE_ME_OPTIONAL
  LANGSMITH_API_KEY: REPLACE_ME_OPTIONAL
